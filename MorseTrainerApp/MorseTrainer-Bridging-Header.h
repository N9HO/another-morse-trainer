//
// Exposes the vendored Carrier Wave CW decoder core (pure C99, compiled
// directly into the app target from Sources/CWDecoderCore) to Swift.
// The core's headers are imported here once the vendored files land; until
// then this header is deliberately empty so the target builds unchanged.
//
